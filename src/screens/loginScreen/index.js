import React from 'react';
import {View} from 'react-native';
import TextWrapper from '../../components/textWrapper';

const LoginScreen = () => (
  <View>
    <TextWrapper text="Hello" />
  </View>
);

export default LoginScreen;
